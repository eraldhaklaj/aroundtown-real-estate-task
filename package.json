{
  "name": "aroundtown-property-assistant",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "predev": "node scripts/check-env.mjs",
    "dev": "concurrently --kill-others-on-fail -n server,client -c blue,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "prebuild": "node scripts/check-env.mjs",
    "build": "npm run build -w client",
    "start": "npm run build && npm run start -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "10.0.5"
  }
}
